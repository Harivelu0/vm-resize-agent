services:
  postgres:
    image: postgres:15
    container_name: demo-postgres
    environment:
      POSTGRES_DB: weatherdb
      POSTGRES_USER: demo
      POSTGRES_PASSWORD: demo123
    ports:
      - "5432:5432"
